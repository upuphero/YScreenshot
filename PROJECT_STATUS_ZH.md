# YScreenshot 项目状态与发展进度

> 更新时间：2026-08-03  
> 当前工作分支：`dev`  
> 本文档根据 [README.md](README.md)、[DEVELOPMENT_PLAN.md](DEVELOPMENT_PLAN.md)、[DEVELOPMENT_LOG.md](DEVELOPMENT_LOG.md) 和 [CONTRIBUTING.md](CONTRIBUTING.md) 整理。

## 一、当前状态概览

YScreenshot 是一个超轻量级 Windows 截图工具，使用 C#、WinForms 和 .NET Framework 4.8 构建。应用以一个无标题栏的置顶工具条和托盘图标为主要界面，截图结果只写入剪贴板，不自动保存图片文件。

当前功能实现状态如下：

| 功能或目标 | 状态 | 说明 |
| --- | --- | --- |
| 全屏截图 | 已实现 | 支持整个虚拟屏幕和多显示器场景。 |
| 矩形区域截图 | 已实现 | 支持鼠标拖选、尺寸提示、Esc 取消和截图前自动隐藏工具条。 |
| 滚动截图 | 已实现 | 先框选固定区域，再由用户手动滚动；程序按 200 ms 间隔捕获、重叠检测和拼接，并支持手动停止。 |
| 剪贴板输出 | 已实现 | 所有截图通过 `ClipboardWriter` 写入剪贴板，不自动生成图片文件。 |
| 工具条与托盘 | 已实现 | 支持拖动、隐藏/折叠、恢复和退出。 |
| 全局热键 | 已实现 | 默认支持全屏、矩形和滚动截图，并可通过设置界面重新绑定。 |
| 多显示器与 DPI | 已实现 | 使用虚拟屏幕坐标和 Per-Monitor-V2 DPI 感知。 |
| 设置界面 | 已实现 | 支持热键、开机启动和反馈样式配置。 |
| 截图历史 | 已实现 | 内存环形缓冲区保留最近 5 次截图，可重新复制到剪贴板。 |
| 可扩展截图模式 | 已实现 | 新模式实现 `ICaptureMode` 并注册到 `CaptureModeRegistry` 即可接入。 |
| 自动化编译与测试 | 已完成 | 使用 .NET SDK 8.0.423 完成 Release 编译，0 警告、0 错误；19/19 个单元测试通过。 |
| 手工验收与性能指标 | 待执行 | 需要在具备构建工具链的 Windows 环境中完成。 |
| Release 打包验证 | 部分完成 | 已生成 Release `YScreenshot.exe`；二进制大小、启动时间、内存和文件写入仍待手工测量。 |

## 二、已完成的发展阶段

### Phase 1：核心 MVP —— 已完成实现，自动化验证通过，待手工验收

已完成内容：

- 建立 `YScreenshot.sln` 和 4 个 SDK-style `net48` 项目。
- 抽取 `ICaptureMode` 与 `CaptureModeRegistry`，形成可扩展的截图模式架构。
- 实现全屏截图和矩形区域截图。
- 实现无标题栏、可拖动、可隐藏/折叠的工具条。
- 接入 `PrintScreen` 和 `Ctrl+Shift+A` 全局热键。
- 截图前自动隐藏工具条，截图完成后恢复，避免工具条出现在截图中。
- 使用 `GeometryUtil`、`MonitorHelper` 和 Per-Monitor-V2 manifest 处理多显示器及混合 DPI 坐标。
- 增加托盘图标，作为恢复工具条和退出应用的备用入口。
- 为几何计算和截图模式注册表增加单元测试。

### Phase 2：滚动截图 —— 已完成实现，自动化验证通过，待真实窗口验收

已完成内容：

- `ScrollingCapture` 复用矩形截图的全屏选择流程，让用户先框选固定的屏幕区域。
- 选区确定后保留一个鼠标穿透的蓝色引导边框，并按 200 ms 间隔捕获该区域；滚动动作完全由用户通过鼠标控制，边框不会进入结果图。
- 逐帧捕获并使用 `FrameStitcher` 做精确像素行重叠检测和纵向拼接。
- 用户暂停滚动时，重复帧会被忽略，不会把短暂停顿误判为内容结束。
- 支持再次按滚动热键来停止并保留已拼接结果（周期捕获期间工具条保持隐藏）。
- 增加约 20,000 像素的高度上限和 600 帧安全上限，避免长时间运行导致资源失控。
- 为部分重叠、无重叠、完全重复和三帧连续拼接场景增加合成位图测试。

已知适用边界：固定区域捕获和像素行匹配属于尽力而为的方案。用户滚动时内容必须保持在选定屏幕区域下方；动态动画、视频、闪烁光标或实时广告可能影响重叠判断。Esc 只在框选阶段取消，进入周期捕获后使用重复触发滚动热键停止。

### Phase 3：可扩展性与完善 —— 已完成实现，自动化验证通过，待打包验收

已完成内容：

- 新增托盘菜单中的 Settings 对话框。
- 支持热键重新绑定、输入校验和保存后即时重新注册热键，无需重启。
- 支持通过当前用户注册表项控制 Windows 开机启动，无需管理员权限。
- 支持 Toast、TrayBalloon 和 None 三种截图反馈方式。
- 增加最近截图菜单，可按时间和模式查看最近 5 次记录并重新复制截图。
- 在 `CONTRIBUTING.md` 中记录新增截图模式的接入流程和长期维护约定。
- 在 `README.md` 中补充滚动截图限制、手工测试矩阵和 Release 打包检查项。

## 三、当前阻塞项与风险

### 1. 自动化验证已完成，手工验收仍待执行

当前已完成 NuGet 还原、Release 编译和自动化单元测试，但尚未完成真实桌面环境中的交互、滚动截图和性能指标验收。因此，当前状态是“构建与自动化测试通过”，不是“全部功能验收完成”。

项目使用 `Microsoft.NETFramework.ReferenceAssemblies` 提供 .NET Framework 4.8 引用程序集，所以按照 README 的说明，通常只需安装 .NET SDK 8.0 或更高版本即可驱动构建，不一定需要完整的 Visual Studio 或 .NET Framework Developer Pack。

### 2. 滚动截图的真实应用兼容性

滚动截图依赖固定屏幕区域和相邻帧之间存在稳定的像素行重叠。以下场景需要重点验证：

- Electron 或其他自定义滚动容器；
- 页面内容在滚动期间持续刷新；
- 视频、动画、闪烁光标或轮播广告；
- 多层嵌套滚动区域；
- 目标窗口在捕获期间移动、调整大小或离开选定区域。

### 3. 性能指标尚未有证据

以下指标仍需在 Release 构建后实测并记录：

- 冷启动到托盘和工具条就绪：小于 300 ms；
- 热键触发到剪贴板可用：总体目标小于 150 ms；Phase 1 的功能验收目标约为 100 ms；
- 空闲工作集：小于 40 MB；
- 发布文件总大小：小于约 5 MB；
- 正常截图期间不产生非预期磁盘写入，设置文件只在退出或保存设置时写入。

## 四、下一步发展进度

建议按以下顺序推进：

1. **准备构建环境**：安装 .NET SDK 8.0 或更高版本，确认 `dotnet --info` 可用。
2. **完成首次构建**：执行 Release 构建，解决编译、依赖或平台目标问题。
3. **执行自动化测试**：运行 `YScreenshot.Capture.Tests`，保存测试结果和失败详情。
4. **执行 Phase 1 手工验收**：优先验证全屏、矩形、多显示器、混合 DPI、取消操作和工具条不入图。
5. **执行 Phase 2 手工验收**：在浏览器、长文档和聊天窗口中验证滚动、拼接、结束判断和手动停止。
6. **执行 Phase 3 手工验收**：验证设置热键、反馈样式、开机启动、截图历史和输入校验。
7. **完成非功能指标和 ProcMon 检查**：记录启动时间、剪贴板延迟、空闲内存、文件大小和磁盘写入。
8. **根据证据修复问题并回归**：优先处理崩溃、错误截图、拼接缺行/重复和热键失效等高优先级问题。
9. **整理发布材料**：更新开发日志、记录验证环境和已知限制，再决定是否生成便携式 Release 包。

## 五、扩展方向

后续如果需要继续扩展，建议保持现有边界：

- 新截图模式只实现 `ICaptureMode` 并注册到 `Program.cs`，避免把模式逻辑硬编码到工具条、热键或设置界面。
- 复杂的几何和像素处理优先抽成无 UI 的纯逻辑类，并用合成数据测试。
- 继续坚持剪贴板优先，不在未明确需求前加入自动保存或临时图片缓存。
- 若滚动截图需要更高性能，优先将 `FrameStitcher` 中基于 `Bitmap.GetPixel` 的行哈希改为 `LockBits`。
- 只有在实际分发需求出现后，再评估安装器、自动更新或更换运行时技术栈。
