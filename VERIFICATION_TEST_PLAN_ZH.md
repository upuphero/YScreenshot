# YScreenshot 验证与测试计划

> 版本：v1 验证计划  
> 更新时间：2026-08-03  
> 适用分支：`dev`  
> 当前状态：Release 编译和自动化测试已通过，真实桌面交互与性能指标仍待手工验收。

## 一、验证目标

本计划用于确认 YScreenshot 在 Windows 环境中满足以下目标：

- 全屏、矩形和滚动截图功能能够正确工作；
- 截图结果能可靠写入剪贴板，工具条不会出现在截图中；
- 多显示器、混合 DPI 和负坐标场景不会产生偏移或缩放错误；
- 全局热键、托盘菜单、设置、开机启动和截图历史符合预期；
- 滚动截图在目标浏览器和静态文档中没有明显缺行或重复；
- 应用达到启动速度、热键延迟、内存占用、文件大小和磁盘写入目标；
- 发现的问题能够被复现、分级、修复并通过回归测试。

## 二、测试前提与环境

### 必需环境

- Windows 10 或 Windows 11；
- .NET SDK 8.0 或更高版本，用于驱动 `net48` 项目构建；
- 可用的剪贴板查看方式，例如 Paint、Word 或其他图片编辑器；
- 至少一个浏览器和一个长文本/文档窗口；
- 建议准备双显示器，并尽量使用不同 DPI 缩放比例进行验证。

### 建议工具

- `dotnet build`、`dotnet test`；
- 任务管理器，用于观察冷启动和空闲内存；
- PowerShell 或秒表，用于粗略计时；
- Process Monitor，用于确认文件写入行为；
- Paint、Word 或图片查看器，用于粘贴并检查截图结果。

### 构建命令

在仓库根目录执行：

```bash
dotnet build YScreenshot.sln -c Release
dotnet test tests/YScreenshot.Capture.Tests/YScreenshot.Capture.Tests.csproj -c Release
```

如果需要直接启动应用：

```bash
dotnet run --project src/YScreenshot.App/YScreenshot.App.csproj
```

构建失败时应先记录完整命令、SDK 版本、目标框架和错误信息，不应直接将“无法运行”归因于业务代码。

## 三、自动化测试计划

### 3.1 构建验证

| 编号 | 操作 | 预期结果 | 证据 |
| --- | --- | --- | --- |
| B-01 | 执行 Release 构建 | 解决方案成功编译，无错误。 | 构建日志、SDK 版本 |
| B-02 | 检查 Release 输出 | 生成 `YScreenshot.App` 及相关项目输出。 | 输出目录清单 |
| B-03 | 在干净构建条件下重复构建 | 不依赖未记录的本地临时文件或手工修改。 | 第二次构建日志 |

### 3.2 单元测试

| 编号 | 覆盖范围 | 重点断言 |
| --- | --- | --- |
| U-01 | `GeometryUtilTests` | 矩形规范化、裁剪和坐标转换正确。 |
| U-02 | `GeometryUtilTests` | 负坐标副屏和跨显示器区域不会产生偏移。 |
| U-03 | `CaptureModeRegistryTests` | 截图模式可注册、按 ID 查找，并保持注册约束。 |
| U-04 | `FrameStitcherTests` | 部分重叠帧能正确拼接，无重叠帧不会丢失内容。 |
| U-05 | `FrameStitcherTests` | 完全重复帧能识别为无进展，避免重复追加。 |
| U-06 | `FrameStitcherTests` | 三帧合成结果与预期合成位图逐字节一致。 |

单元测试必须在测试命令退出码为 0 后才算通过。仅有测试项目能够编译而没有执行测试，不能替代单元测试通过。

## 四、Phase 1 功能测试

| 编号 | 场景与操作 | 预期结果 |
| --- | --- | --- |
| P1-01 | 启动应用并观察工具条 | 工具条显示为无标题栏的薄条，按钮和托盘图标可用。 |
| P1-02 | 拖动工具条，再点击 Hide 和边缘折叠标签 | 工具条位置可保存；Hide 折叠到边缘；点击标签可恢复。 |
| P1-03 | 点击 Full Screen 或按 `PrintScreen` | 工具条先隐藏，虚拟屏幕被完整复制到剪贴板，工具条随后恢复。 |
| P1-04 | 点击 Rectangle 或按 `Ctrl+Shift+A`，拖选一个区域 | 选区尺寸反馈正确，释放鼠标后剪贴板只有选区内容。 |
| P1-05 | 进入矩形选择后按 `Esc` | 操作静默取消，不写入错误结果，工具条仍能恢复。 |
| P1-06 | 单显示器上重复全屏和矩形截图 | 截图无黑边、偏移、额外区域或工具条残影。 |
| P1-07 | 双显示器、不同 DPI 下执行全屏截图 | 截图覆盖整个虚拟屏幕，各显示器分辨率和比例正确。 |
| P1-08 | 从一个显示器拖选到另一个显示器 | 选区边界和最终裁剪结果与鼠标拖选范围一致。 |
| P1-09 | 在工具条展开和折叠状态分别截图 | 两种状态下工具条都不出现在截图结果中。 |
| P1-10 | 通过托盘 Exit 退出，重新启动应用 | 应用能正常退出；重新启动后工具条回到上次保存的位置。 |

## 五、Phase 2 滚动截图测试

### 5.1 正常流程

| 编号 | 场景与操作 | 预期结果 |
| --- | --- | --- |
| P2-01 | 触发 Scrolling，框选长篇静态网页中的固定区域，然后手动滚动 | 蓝色选区边框持续显示且不拦截鼠标；程序每约 200 ms 捕获一次固定区域，最终生成一张连续长图并写入剪贴板。 |
| P2-02 | 对长文本、Notepad 文档或聊天记录先框选区域，再手动滚动 | 拼接处没有明显缺行或重复，内容保持在选定区域下方。 |
| P2-03 | 手动滚动过程中暂停数秒 | 重复帧被忽略，暂停不会自动结束捕获。 |
| P2-04 | 捕获过程中再次按滚动热键（默认 `Ctrl+Shift+S`） | 能及时停止并保留已经拼接的内容，不丢弃已有结果。 |
| P2-05 | 框选后改变滚动方向或滚动幅度 | 记录重叠检测和拼接表现；固定区域仍按屏幕坐标捕获。 |
| P2-06 | 在框选阶段按 `Esc` | 选择取消，不进入周期捕获循环，也不产生剪贴板结果。 |
| P2-07 | 停止滚动截图并粘贴结果 | 结果中不应出现蓝色引导边框或工具条。 |

### 5.2 兼容性与边界

至少补充以下观察记录：

- 原生 Win32、普通浏览器、Electron 或自定义滚动容器；
- 页面包含动画、视频、闪烁光标或动态广告；
- 用户暂停、快速滚动、反向滚动或滚动幅度不一致；
- 内容高度超过约 20,000 像素或捕获帧数超过 600 帧；
- 选区期间或捕获中目标窗口移动、关闭、失去响应或改变大小。

这些场景不一定都要求 v1 完全支持，但必须记录实际表现、是否可复现，以及是否需要在后续版本改进。滚动截图进入周期捕获后应使用重复滚动热键停止；`Esc` 只负责取消矩形选区。

## 六、Phase 3 设置与完善功能测试

| 编号 | 场景与操作 | 预期结果 |
| --- | --- | --- |
| P3-01 | 托盘菜单打开 Settings | 设置对话框正常打开，且不改变工具条的无标题栏设计。 |
| P3-02 | 录入新的有效热键并保存 | 新热键立即生效，旧热键不再触发，无需重启。 |
| P3-03 | 清空热键或输入无效组合后保存 | 显示校验提示，设置不应错误保存或关闭。 |
| P3-04 | 将反馈切换为 Toast、TrayBalloon、None | 三种模式分别显示工具条提示、托盘气泡或不显示反馈，剪贴板功能不受影响。 |
| P3-05 | 开启 Start with Windows | 当前用户的 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 中出现正确的 `YScreenshot` 值。 |
| P3-06 | 关闭 Start with Windows | 对应注册表值被移除，且不需要管理员权限。 |
| P3-07 | 完成 3 次以上不同模式截图并打开 Recent Captures | 按时间倒序显示最近记录，并显示时间和模式名称。 |
| P3-08 | 点击历史记录中的旧截图 | 旧截图重新复制到剪贴板，不重新执行截图。 |
| P3-09 | 退出并重新启动后查看历史记录 | 确认历史记录是内存数据，按产品约定不要求跨进程持久化。 |

## 七、非功能测试与发布验收

### 7.1 性能指标

每项指标至少测量 3 次，记录最好值、最差值和平均值，并注明测试硬件和系统状态。

| 指标 | 目标 | 测量方式 |
| --- | --- | --- |
| 冷启动 | 到工具条和托盘就绪小于 300 ms | 从启动进程到 UI 可操作计时。 |
| 全屏/矩形热键延迟 | 总体目标小于 150 ms；Phase 1 功能目标约 100 ms | 从触发热键到剪贴板图像可粘贴计时。 |
| 空闲内存 | 工作集小于 40 MB | 应用启动后静置，分别记录展开和折叠状态。 |
| Release 文件大小 | 应用及卫星 DLL 总计小于约 5 MB | 检查 `src/YScreenshot.App/bin/Release/net48/` 输出。 |
| 滚动资源边界 | 不超过约 20,000 像素或 200 次迭代 | 观察最终停止行为和内存是否异常增长。 |

### 7.2 磁盘写入检查

使用 Process Monitor 过滤 `YScreenshot.exe`，覆盖以下操作：启动、三种截图、设置保存、托盘退出。预期行为如下：

- 截图过程中不写入图片文件、临时文件或缓存文件；
- 正常允许的设置写入位置为 `%AppData%\YScreenshot\settings.json`；
- 设置文件只应在设置保存或应用退出等约定时机写入；
- 若发现其他路径写入，必须记录路径、操作类型、触发步骤和是否为框架行为。

### 7.3 发布包检查

- 使用 Release 配置构建，而不是 Debug 配置；
- 确认输出中包含运行所需的应用程序集和卫星 DLL；
- 在一台没有开发工具的目标 Windows 机器上验证可启动性；
- 确认 .NET Framework 4.8 运行环境符合项目假设；
- 保存发布目录清单、文件大小、版本信息和启动结果。

## 八、测试记录模板

每次验证建议记录以下信息：

```text
测试日期：
测试人：
分支/提交：
Windows 版本：
CPU / 内存 / 显示器与 DPI：
.NET SDK 版本：

构建结果：通过 / 失败
自动化测试：通过 / 失败（通过数、失败数）
手工测试范围：
非功能指标：
ProcMon 结果：

发现的问题：
复现步骤：
严重级别：Blocker / High / Medium / Low
关联日志或截图：
回归结果：
```

## 九、通过标准与问题分级

### 通过标准

只有同时满足以下条件，才能将 v1 标记为“验证通过”：

1. Release 构建成功；
2. 所有现有单元测试通过；
3. Phase 1、Phase 2 和 Phase 3 的必测用例通过；
4. 没有未处理的崩溃、错误截图、剪贴板丢失、严重坐标错误或滚动结果破坏性问题；
5. 非功能指标已测量并记录，偏差已有明确结论；
6. ProcMon 检查没有未解释的非预期文件写入；
7. 已知限制已同步到 README 或开发日志。

### 问题分级

- **Blocker**：无法构建或启动、应用崩溃、截图数据丢失、无法退出，或出现严重的非预期文件写入。
- **High**：核心截图模式不可用、结果明显错误、跨显示器坐标严重偏移、热键或设置完全失效。
- **Medium**：特定窗口类型兼容性差、滚动拼接偶发缺行/重复、性能指标明显超标但不影响基本使用。
- **Low**：提示文案、布局细节或非核心体验问题，不影响截图正确性和数据安全。
